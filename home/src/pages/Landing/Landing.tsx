import s from "./Landing.module.scss";

import React from 'react'

type Props = {}

const Landing = (props: Props) => {
  return (
    <div className={s.titlePart}>Landing</div>
  )
}

export default Landing